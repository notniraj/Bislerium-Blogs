using Microsoft.AspNetCore.SignalR;

namespace BIsleriumCW.Services
{
    public class CommentHub : Hub
    {
        //public void SendCommentNotification(string userId, string comment)
        //{
        //    // Send the comment notification to the specified user
        //    Clients.User(userId).receiveCommentNotification(comment);
        //}
    }
}
